# Dependencies
node_modules/

# Build outputs
dist/
release/
build/

# TTS server (Portable Mode – too large for git, ~4-6 GB)
tts-server/
# AMD ROCm variant of the TTS server env (built by setup-tts-server-amd.ps1)
tts-server-amd/
python_embedded/

# Test outputs
*.wav

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Models (too large for git)
models/

# Asset pack (downloaded at build time, too large for git)
asset-pack/

# Static pack (copied from Battlemap public/ at build time, too large for git)
static-pack/

# Steam build output
steam_output/
steam_appid.txt

# Splash video (62 MB, too large for git -- place manually)
assets/TironaFading.mp4

# Character-entry cutscene (delivered via Vercel Blob -> asset-pack, not git/ASAR)
# Raw source + compressed staging copy; both too large for git.
assets/ENTERTIRONA.mp4
cutscenes/

# Voice reference samples
voices/
# Pre-normalization originals (kept until the -18 LUFS wav set is proven)
voices-originals-backup/
# Raw voice takes dropped in for conversion (mp3 -> -18 LUFS 44.1k mono wav).
# Source material only; the converted wavs in voices/ are what ship.
importassets/

# Stale/generated docs
BUILD_SUCCESS.md
CHATTERBOX_*.md
IMPLEMENTATION_*.md
QUICK_START.md
SETUP_COMPLETE.md
TTS_INTEGRATION_*.md
TTS_NEXT_STEPS.md
TTS_TESTING_GUIDE.md

# Obsolete scripts
scripts/build-chatterbox.ps1

# Environment
.env
.env.local
