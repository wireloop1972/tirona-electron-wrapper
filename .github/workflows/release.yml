# Optional: Automated GitHub Releases
# Uncomment and configure when ready for CI/CD

# name: Release

# on:
#   push:
#     tags:
#       - 'v*'

# jobs:
#   release:
#     runs-on: ${{ matrix.os }}

#     strategy:
#       matrix:
#         os: [windows-latest, macos-latest]

#     steps:
#       - name: Check out Git repository
#         uses: actions/checkout@v4

#       - name: Install Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - name: Install dependencies
#         run: npm install

#       - name: Build and release
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         run: |
#           npm run build
#           npm run package

# This workflow will:
# 1. Trigger on version tags (e.g., v1.0.1)
# 2. Build on both Windows and macOS
# 3. Automatically publish to GitHub Releases
# 4. Include all necessary files for auto-update

# To enable:
# 1. Uncomment this file
# 2. Push a tag: git tag v1.0.1 && git push origin v1.0.1
# 3. GitHub Actions will build and release automatically

